# Host canonicalization. The site is served from the apex; the www host is a
# custom domain on the same Pages project and must 301 to the apex so Google
# indexes one host. Only the www host is matched, so the apex can never loop.
https://www.healthcarepestreference.org/* https://healthcarepestreference.org/:splat 301
