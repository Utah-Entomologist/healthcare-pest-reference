{
  "name": "corrections-pest-reference",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "National authoritative reference for pest control requirements in jails, prisons, and detention facilities",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "postbuild": "node scripts/validate-jsonld.mjs && node scripts/check-links.mjs",
    "validate": "node scripts/validate-jsonld.mjs && node scripts/check-links.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^22.20.2",
    "schema-dts": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
